"use client";
import{useActionState,useState}from"react";import{AlertTriangle,Plus,Stethoscope,X}from"lucide-react";import{cancelEncounter,createConsultation,type ClinicalState}from"@/app/clinical/actions";
type Patient={id:string;mrn:string;first_name:string;last_name:string};type Allergy={patient_id:string;substance:string;reaction:string|null};type Encounter={id:string;encounter_no:string;status:string;service_date:string;patient_id:string};const initial:ClinicalState={ok:false,message:""};
export function ClinicalWorkspace({patients,allergies,encounters,facilityId}:{patients:Patient[];allergies:Allergy[];encounters:Encounter[];facilityId:string}){const[open,setOpen]=useState(false);return <><div className="toolbar"><button className="btn btn-primary" onClick={()=>setOpen(true)}><Plus size={15}/>Start consultation</button></div><section className="card"><div className="card-header"><h3>Recent consultations</h3><span className="badge blue">Live data</span></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Encounter</th><th>Patient</th><th>Date</th><th>Allergy alert</th><th>Status / Action</th></tr></thead><tbody>{encounters.map(e=>{const patient=patients.find(x=>x.id===e.patient_id);const alerts=allergies.filter(x=>x.patient_id===e.patient_id);return <tr key={e.id}><td><strong>{e.encounter_no}</strong></td><td className="name-cell"><strong>{patient?`${patient.last_name}, ${patient.first_name}`:"Patient"}</strong><span>{patient?.mrn}</span></td><td>{e.service_date}</td><td>{alerts.length?<span className="allergy-alert"><AlertTriangle size={13}/>{alerts.map(x=>x.substance).join(", ")}</span>:"None documented"}</td><td><span className="badge green">{e.status.replaceAll("_"," ")}</span>{e.status!=="completed"&&e.status!=="cancelled"&&<form action={cancelEncounter} className="cancel-record"><input type="hidden" name="encounter_id" value={e.id}/><input required name="reason" aria-label="Cancellation reason" placeholder="Cancellation reason"/><button className="table-action">Cancel encounter</button></form>}</td></tr>})}{!encounters.length&&<tr><td colSpan={5} className="empty-state">No consultations recorded yet.</td></tr>}</tbody></table></div></section>{open&&<ConsultationDialog patients={patients} allergies={allergies} facilityId={facilityId} close={()=>setOpen(false)}/>}</>}
function ConsultationDialog({patients,allergies,facilityId,close}:{patients:Patient[];allergies:Allergy[];facilityId:string;close:()=>void}){const[state,action,pending]=useActionState(createConsultation,initial);const[patientId,setPatientId]=useState("");const active=allergies.filter(a=>a.patient_id===patientId);return <div className="modal-backdrop"><section className="modal clinical-modal" role="dialog" aria-modal="true"><div className="modal-head"><div><p className="eyebrow">Outpatient care</p><h3>New consultation</h3></div><button className="icon-btn" onClick={close} aria-label="Close"><X size={17}/></button></div><form action={action} className="patient-form"><input type="hidden" name="facility_id" value={facilityId}/><label className="wide">Patient<select required name="patient_id" value={patientId} onChange={e=>setPatientId(e.target.value)}><option value="">Select a registered patient</option>{patients.map(patient=><option key={patient.id} value={patient.id}>{patient.mrn} · {patient.last_name}, {patient.first_name}</option>)}</select></label>{patientId&&<div className={active.length?"allergy-panel wide":"notice wide"}>{active.length?<><AlertTriangle size={17}/><strong>Allergy alert: {active.map(a=>`${a.substance}${a.reaction?` (${a.reaction})`:""}`).join(", ")}</strong></>:"No active allergies documented. Confirm with the patient."}</div>}<label className="wide">Chief complaint<textarea required name="chief_complaint" rows={2}/></label><label>Systolic BP<input type="number" name="systolic" min="40" max="300"/></label><label>Diastolic BP<input type="number" name="diastolic" min="20" max="200"/></label><label>Temperature °C<input type="number" step="0.1" name="temperature" min="25" max="45"/></label><label>SpO₂ %<input type="number" name="spo2" min="40" max="100"/></label><label>New allergy<input name="allergy_substance" placeholder="Leave blank if none"/></label><label>Allergic reaction<input name="allergy_reaction"/></label><label className="wide">Clinical/SOAP note<textarea required name="soap_note" rows={5}/></label><label className="wide">Working diagnosis<input name="diagnosis"/></label>{state.message&&<div className={state.ok?"form-success wide":"form-error wide"}>{state.message}</div>}<div className="form-actions wide"><button type="button" className="btn btn-secondary" onClick={close}>Cancel</button><button disabled={pending} className="btn btn-primary"><Stethoscope size={15}/>{pending?"Saving…":"Save consultation"}</button></div></form></section></div>}
