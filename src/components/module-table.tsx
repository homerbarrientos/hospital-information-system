export type ModuleRow={primary:string;secondary:string;detail:string;status:string;tone?:string};
export function ModuleTable({headers,rows}:{headers:[string,string,string,string];rows:ModuleRow[]}){return <div className="card table-wrap"><table className="data-table"><thead><tr>{headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map(r=><tr key={`${r.primary}-${r.secondary}`}><td className="name-cell"><strong>{r.primary}</strong><span>{r.secondary}</span></td><td>{r.detail}</td><td><span className={`badge ${r.tone??"blue"}`}>{r.status}</span></td><td><button className="btn btn-secondary">Open workflow</button></td></tr>)}</tbody></table></div>}
