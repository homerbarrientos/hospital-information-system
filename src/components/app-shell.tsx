"use client";
import Link from"next/link";import{usePathname}from"next/navigation";import{Activity,Bell,CalendarDays,CircleDollarSign,ClipboardPlus,FileChartColumn,Gauge,HeartPulse,LogOut,Menu,Settings,ShieldCheck,UsersRound}from"lucide-react";
const main=[["/","Dashboard",Gauge],["/patients","Patients",UsersRound],["/queue","Appointments & Queue",CalendarDays],["/clinical","OPD Clinical",ClipboardPlus],["/billing","Billing & Cashier",CircleDollarSign],["/reports","Reports",FileChartColumn]]as const;const admin=[["/administration","Administration",Settings],["/audit","Audit & Compliance",ShieldCheck]]as const;
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname();return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark"><HeartPulse size={23}/></div><div><h1>Hospital ONE</h1><p>Government Level 1</p></div></div><p className="nav-label">Patient operations</p><nav className="nav-list">{main.map(([h,l,I])=><Link key={h} href={h} className={`nav-item ${path===h?"active":""}`}><I size={16}/>{l}</Link>)}</nav><p className="nav-label">Governance</p><nav className="nav-list">{admin.map(([h,l,I])=><Link key={h} href={h} className={`nav-item ${path===h?"active":""}`}><I size={16}/>{l}</Link>)}</nav><div className="profile-card"><div className="avatar">HB</div><div><strong>Homer Barrientos</strong><span>Hospital Administrator</span></div></div></aside><main className="app-main"><header className="topbar"><div className="facility"><strong>Government Hospital Pilot</strong><span>OPD Operations · Philippine Time</span></div><div className="top-actions"><button className="icon-btn" aria-label="Open navigation"><Menu size={17}/></button><button className="icon-btn" aria-label="Notifications"><Bell size={17}/></button><button className="icon-btn" aria-label="Log out"><LogOut size={17}/></button></div></header><div className="content">{children}</div></main></div>}
export function PageHeading({eyebrow,title,description,action}:{eyebrow:string;title:string;description:string;action?:React.ReactNode}){return <div className="page-heading"><div><p className="eyebrow">{eyebrow}</p><h2>{title}</h2><p className="subtext">{description}</p></div>{action}</div>}
export function DemoNotice(){return <div className="notice"><Activity size={16}/><div><strong>Safe demonstration workspace</strong>Synthetic records are shown until Supabase is connected. Do not enter live patient data here.</div></div>}
