"use client";
import{useEffect,useState}from"react";import{FilePenLine,X}from"lucide-react";import{amendConsultation,cancelEncounter}from"@/app/clinical/actions";import{createClient}from"@/lib/supabase/client";
type Encounter={id:string;encounter_no:string;status:string;service_date:string;patient_id:string};type Patient={id:string;mrn:string;first_name:string;last_name:string};type Allergy={patient_id:string;substance:string;reaction:string|null};type Detail={complaint:string;soap:string;diagnosis:string;version:number;versions:Array<{version:number;amendment_reason:string|null;created_at:string}>;vitals:Array<{code:string;value:number;unit:string}>};
export function EncounterDetailModal({encounter,patient,allergies,onClose}:{encounter:Encounter;patient?:Patient;allergies:Allergy[];onClose:()=>void}){const[detail,setDetail]=useState<Detail|null>(null);const[editing,setEditing]=useState(false);useEffect(()=>{let active=true;(async()=>{const db=createClient();const[{data:notes},{data:vitals},{data:diagnoses}]=await Promise.all([db.from("clinical_notes").select("id,current_version").eq("encounter_id",encounter.id).order("created_at",{ascending:false}).limit(1),db.from("vital_observations").select("code,value,unit").eq("encounter_id",encounter.id),db.from("diagnoses").select("description").eq("encounter_id",encounter.id).order("created_at",{ascending:false}).limit(1)]);const note=notes?.[0];const{data:versions}=note?await db.from("clinical_note_versions").select("version,content,amendment_reason,created_at").eq("note_id",note.id).order("version",{ascending:false}):{data:[]};const latest=versions?.[0];const content=(latest?.content||{})as{chief_complaint?:string;soap_note?:string};if(active)setDetail({complaint:content.chief_complaint||"",soap:content.soap_note||"",diagnosis:diagnoses?.[0]?.description||"",version:note?.current_version||1,versions:(versions||[]).map(v=>({version:v.version,amendment_reason:v.amendment_reason,created_at:v.created_at})),vitals:vitals||[]})})();return()=>{active=false}},[encounter.id]);const activeAllergies=allergies.filter(a=>a.patient_id===encounter.patient_id);return <div className="modal-backdrop"><section className="modal encounter-detail" role="dialog" aria-modal="true" aria-label="Consultation details"><div className="modal-head"><div><p className="eyebrow">Consultation record</p><h3>{encounter.encounter_no}</h3></div><button className="icon-btn" onClick={onClose} aria-label="Close"><X size={17}/></button></div>{!detail?<div className="detail-loading">Loading saved clinical record…</div>:<div className="detail-body"><div className="detail-summary"><Info label="Patient" value={patient?`${patient.last_name}, ${patient.first_name} · ${patient.mrn}`:"Patient"}/><Info label="Date" value={encounter.service_date}/><Info label="Status" value={encounter.status.replaceAll("_"," ")}/><Info label="Version" value={`v${detail.version}`}/></div><Info label="Allergies" value={activeAllergies.length?activeAllergies.map(a=>`${a.substance}${a.reaction?` (${a.reaction})`:""}`).join(", "):"None documented"}/><Info label="Vital signs" value={detail.vitals.length?detail.vitals.map(v=>`${v.code}: ${v.value} ${v.unit}`).join(" · "):"None recorded"}/>{!editing?<><Info label="Chief complaint" value={detail.complaint||"Not recorded"}/><Info label="Clinical / SOAP note" value={detail.soap||"Not recorded"}/><Info label="Working diagnosis" value={detail.diagnosis||"Not recorded"}/><div className="detail-actions">{encounter.status!=="cancelled"&&<button className="btn btn-primary" onClick={()=>setEditing(true)}><FilePenLine size={15}/>Modify record</button>}</div></>:<form action={amendConsultation} className="detail-form"><input type="hidden" name="encounter_id" value={encounter.id}/><label>Chief complaint<textarea required name="chief_complaint" defaultValue={detail.complaint} rows={2}/></label><label>Clinical / SOAP note<textarea required name="soap_note" defaultValue={detail.soap} rows={5}/></label><label>Working diagnosis<input name="diagnosis" defaultValue={detail.diagnosis}/></label><label>Reason for modification<textarea required name="reason" rows={2}/></label><div className="form-actions"><button type="button" className="btn btn-secondary" onClick={()=>setEditing(false)}>Cancel edit</button><button className="btn btn-primary">Save amendment</button></div></form>}<details className="version-history"><summary>Modification history ({detail.versions.length})</summary>{detail.versions.map(v=><div key={v.version}>Version {v.version} · {new Date(v.created_at).toLocaleString()} {v.amendment_reason&&`· ${v.amendment_reason}`}</div>)}</details>{encounter.status!=="completed"&&encounter.status!=="cancelled"&&<form action={cancelEncounter} className="detail-cancel"><input type="hidden" name="encounter_id" value={encounter.id}/><label>Cancellation reason<textarea required name="reason" rows={2}/></label><button className="btn btn-danger">Cancel encounter</button></form>}</div>}</section></div>}
function Info({label,value}:{label:string;value:string}){return <div className="detail-field"><span>{label}</span><strong>{value}</strong></div>}
