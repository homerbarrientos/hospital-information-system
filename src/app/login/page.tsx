import{HeartPulse}from"lucide-react";import{signIn,signUp}from"./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const p=await searchParams;return <main className="auth-page"><section className="auth-card"><div className="auth-brand"><span><HeartPulse/></span><div><h1>Hospital ONE</h1><p>Infirmary Core</p></div></div><h2>Sign in to your facility</h2><p className="subtext">The first account created becomes the pilot facility administrator.</p>{p.error&&<div className="form-error">{p.error}</div>}{p.message&&<div className="form-success">{p.message}</div>}<form className="form-stack"><label>Full name<input name="full_name" autoComplete="name" placeholder="Juan Dela Cruz"/></label><label>Email<input required type="email" name="email" autoComplete="email"/></label><label>Password<input required minLength={8} type="password" name="password" autoComplete="current-password"/></label><div className="form-actions"><button formAction={signIn} className="btn btn-primary">Sign in</button><button formAction={signUp} className="btn btn-secondary">Create account</button></div></form><p className="security-note">Use synthetic patient information during MVP testing.</p></section></main>}
