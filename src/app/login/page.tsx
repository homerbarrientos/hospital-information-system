import{HeartPulse}from"lucide-react";import{signIn,signUp}from"./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string;next?:string}>}){const p=await searchParams;return <main className="auth-page"><section className="auth-card"><div className="auth-brand"><span><HeartPulse/></span><div><h1>Hospital ONE</h1><p>Infirmary Core</p></div></div><h2>Sign in to your facility</h2><p className="subtext">Use your work email or hospital Employee ID. Staff accounts are issued by an administrator.</p>{p.error&&<div className="form-error">{p.error}</div>}{p.message&&<div className="form-success">{p.message}</div>}<form className="form-stack"><input type="hidden" name="next" value={p.next||""}/><label>Full name <small>(pilot administrator setup only)</small><input name="full_name" autoComplete="name" placeholder="Juan Dela Cruz"/></label><label>Email or Employee ID<input required name="identifier" autoComplete="username" placeholder="staff@hospital.example or EMP-00015"/></label><label>Password<input required minLength={8} type="password" name="password" autoComplete="current-password"/></label><div className="form-actions"><button formAction={signIn} className="btn btn-primary">Sign in</button><button formAction={signUp} className="btn btn-secondary">Create pilot administrator</button></div></form><p className="security-note">Temporary Employee ID passwords must be changed at first login.</p></section></main>}
