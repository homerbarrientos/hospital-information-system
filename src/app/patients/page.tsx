import{redirect}from"next/navigation";import{AppShell,PageHeading}from"@/components/app-shell";import{PatientWorkspace,type Patient}from"@/components/patient-workspace";import{createClient}from"@/lib/supabase/server";
export default async function Patients(){const supabase=await createClient();const{data}=await supabase.auth.getClaims();const userId=data?.claims.sub;if(!userId)redirect("/login");const[{data:assignments},{data:patients,error}]=await Promise.all([supabase.from("user_roles").select("facility_id").eq("user_id",userId).eq("active",true).limit(1),supabase.from("patients").select("id,mrn,first_name,middle_name,last_name,birth_date,sex_at_birth,phone,email,created_at,version").order("created_at",{ascending:false})]);const facilityId=assignments?.[0]?.facility_id;if(!facilityId)return <AppShell><PageHeading eyebrow="Access required" title="No facility assignment" description="Ask an administrator to assign your account to a facility and role."/><div className="form-error">This user cannot access patient records yet.</div></AppShell>;return <AppShell><PageHeading eyebrow="Master patient index" title="Patients" description="Search first to prevent duplicate patient records."/>{error&&<div className="form-error">{error.message}</div>}<PatientWorkspace patients={(patients||[])as Patient[]} facilityId={facilityId}/></AppShell>}
